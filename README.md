# AudioCutter
 The program cuts a .mp3 audio file into pieces according to indices provided in a .cue file. Audioteka doesn't provide a program for Linux program to cut their audiobooks into chapters. Here's a small script that does exatly that.
